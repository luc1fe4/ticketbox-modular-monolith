# TicketBox Mobile Scanner

This Expo React Native package is a placeholder for the gate check-in staff application.

The future scanner app will use local SQLite through Expo SQLite to keep ticket snapshots and
local check-in events available while offline. QR scanning, sync, and conflict handling are not
implemented in this environment setup stage.
